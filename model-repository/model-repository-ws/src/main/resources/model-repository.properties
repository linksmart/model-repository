# model-repository config

model.repository.url=${model.repository.url}

# validate incoming xml document with schema (to avoid version conflict and xml errors)
model.repository.schema.validation=false